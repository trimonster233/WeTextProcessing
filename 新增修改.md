# 新增修改

## 中文身份证号码 TN 规则

### 修改原因

WeTextProcessing 原有中文 ITN 已支持将口语形式的身份证号码转换成数字号码，例如：

```text
三四零二零三一九三七零幺零幺零五幺X
→ 34020319370101051X
```

但中文 TN 没有对应的反向规则。用于 TTS 时，18 位身份证号码无法被整体识别，会被拆成“11 位电话号码 + 普通数值”，导致后半段按照十、百、千、万等数值单位朗读。

原错误示例：

```text
110101199003078515
→ 幺幺零幺零幺幺九九零零三百零七万八千五百一十五
```

### 修改位置

身份证 TN 判定逻辑添加在：

```text
tn/chinese/rules/cardinal.py
```

回归样例添加在：

```text
tn/chinese/test/data/cardinal.txt
```

现有 `tn/chinese/test/cardinal_test.py` 会自动读取该数据文件，因此没有新增单独的 Python 测试文件。

### 判定方式

满足以下任一格式的连续字符会按照身份证号码处理：

1. 18 位数字；
2. 17 位数字，末位为 `X` 或 `x`。

新增规则：

```python
identity_card = digits**18
identity_card |= digits**17 + (cross("X", "叉") | cross("x", "叉"))
cardinal |= add_weight(identity_card, -2.0)
```

身份证规则使用 `-2.0` 权重，保证完整的 18 位号码优先于 11 位电话号码等其他 `cardinal` 分支，避免号码被拆开处理。

当前只进行格式判定，不校验行政区划、出生日期、顺序码以及末位校验码是否合法。

### 朗读方式

身份证号码按字符逐位朗读，不按普通整数展开：

```text
0 → 零
1 → 一
2 → 二
3 → 三
4 → 四
5 → 五
6 → 六
7 → 七
8 → 八
9 → 九
X/x → 叉
```

身份证中的 `1` 读作“一”，不使用电话号码中的“幺”。

### 示例

纯数字身份证：

```text
输入：110101199003078515
输出：一一零一零一一九九零零三零七八五一五
```

末位为 `X`：

```text
输入：34052419800101001X
输出：三四零五二四一九八零零一零一零零一叉
```

完整句子：

```text
输入：请核对本人的身份证号码：110101199003078515。
输出：请核对本人的身份证号码：一一零一零一一九九零零三零七八五一五。

输入：证件号码34052419800101001X。
输出：证件号码三四零五二四一九八零零一零一零零一叉。
```

